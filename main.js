
// Some cool comment
//
// What does this do 
// interest


/* FAncy multiline commetn*///maybe another

/* actally a multiple
 * line comment
 * the more you know
 */

(3.4) * (2 + thething_) - whatever;

-3.3 + the * whatever + 4 - 2;

false <= "what" + (3 * 2);

"strictest mode";

// one more /* comment */ lk

' silly string thasldfkj allkj ';

// dumb interupt ing commen
// laksjdf

(32 + 34) - someCool_[33].what / 2;

outer.inner + outer[inner + 1] * 2;

34 * (5 + 2) + Math.random(sixtynine /*69*/, "69") + 2;

someFunc() * 2 + [2, 3, 45, what.the] + 3;

// recursive function demonstraction
Date.now([1, "a", '23', a()], inner());

{
	finalish: Boss,
	"string": 7.7,
	2: "string",
};

const hi = Math.random();
const three3hi = 3 * (Math.random() + 1);

let other_Var =  "Some Interesting Sasldfkj ()dl kjasldfkj []";
const what = true;

function someFunction() {}

if (true == 3) {
	// do something interesting
}

while (false) {}

var wackString = 'one line strinasdf " ';


const something = function() {
	// random didid do da
};


function otherFunction(alpha, bravo, charlie) {
	return (three3hi * alpha) * bravo + charlie;
}


onload = hi;

class SomeClassification {
	constructor() {
		// Some comment that doesn't mean anything
		// Does literally nothing
		/*
		 *
		 * another form of comment
		 *
		 */
		3 + 4;

		forest += 45;
	}
	doit() {
		console.log("Hi world" );
	}
}

// Some random comment
//
;



